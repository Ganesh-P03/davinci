class Array {
    function Array new(int size) {
    	return Memory.alloc(size); // returns the array's base address allocated in memory
    }

    method void dispose() {
    	do Memory.deAlloc(this); // frees the array's space from memory
        return;
    }
}
